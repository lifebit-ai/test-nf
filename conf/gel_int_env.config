/*
 * ---------------------------------------------------------------------
 *  lifebit-ai/gel-mongodb-ingestion gel_int_env config file
 * ---------------------------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 * nextflow run main.nf -profile gel_int_env
 */

docker.enabled                          = true

params  {
    db_name                             = "cohort-browser-int"
    aws_param_name_for_mongo_uri        = "/int/nextflow/MONGO_URI"
    param_via_aws                       = true
}